# PDF Tool

This repository hosts Netlify utilities for server-side Agent SDK artifact generation.

## Agent artifact generation

ChatGPT agents remain responsible for text and structured content work through MCP and project JSON blobs. Binary artifact generation moves server-side to a Netlify Agent SDK workflow so generated bytes can be saved directly to Netlify Blobs without exposing provider credentials to clients or passing binary bytes through MCP.

### Responsibilities

- ChatGPT agents: article/content planning, drafts, briefs, metadata, workflow records, and project JSON updates through MCP.
- Netlify Agent SDK artifact utility: generated binary artifacts such as images first, with PDF support intentionally rejected until enabled.
- Normal clients: direct upload paths remain available for standard client-provided files.
- Fallback tools: `save_artifact` and `create_artifact_from_url` remain fallback paths only.

### Reference-only artifact examples

`/Artifacts code examples` on `main` is reference-only. Runtime code must not import from that folder. Runtime artifact persistence lives under `netlify/lib/artifact-core/` and preserves the retained indexes required by the reference implementation:

- `request-artifacts`
- `by-request`
- `by-kind`
- `by-tag`

### Endpoints

- `POST /.netlify/functions/agent-artifact-job`
  - Creates a pending artifact job.
  - Requires `Authorization: Bearer AGENT_RUN_TOKEN`.
- `GET|POST /.netlify/functions/agent-artifact-job-status`
  - Returns pending/running/complete/failed job state.
  - Requires `Authorization: Bearer AGENT_RUN_TOKEN`.
- `POST /.netlify/functions/agent-artifact-worker-background`
  - Internal Netlify background worker entrypoint that runs the Agent SDK workflow and saves the artifact.
  - Requires `Authorization: Bearer AGENT_RUN_TOKEN`.

### Required environment variables

- `AGENT_RUN_TOKEN`: bearer token for internal agent job APIs.
- `OPENAI_API_KEY`: server-only OpenAI API key used by Netlify artifact generation.
- `ARTIFACT_AGENT_PROJECT_ID`: optional additional allowed project ID for future project-specific routing.

Never expose `OPENAI_API_KEY` to browsers or ChatGPT-hosted clients.

### Supported project IDs

The first configured project is:

- `dr-lurie`

### Output limits

Generated artifact output is limited to `5_000_000` bytes before persistence. Image validation also runs inside `saveArtifactBytes()`.
