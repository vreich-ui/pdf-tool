# HTTP reference (generated)

> GENERATED by `scripts/generate-reference.mts` from `netlify/functions/*.ts`, `netlify.toml` and `render-service/src/server.ts`. Do not edit by hand — run `npm run docs:generate`. Method lists are declared in the generator and cross-checked against the string-literal method checks in each file.

Netlify functions: **32**. Every function is reachable at `/.netlify/functions/<name>`; the aliases below come from `netlify.toml`. Background functions (`*-background`) return 202 immediately and run for up to 15 minutes; they are triggered server-to-self with `Authorization: Bearer AGENT_RUN_TOKEN` and the storage grant in the POST body.

## Netlify functions

| Function | Status | Methods | Auth | Storage grant | Alias / schedule | Handler | Purpose |
|---|---|---|---|---|---|---|---|
| `agent-artifact-job-status` | LEGACY | GET, POST | Bearer AGENT_RUN_TOKEN | required | — | `inline (readArtifactJob)` | LEGACY status read. No 12-minute auto-fail, no materializationProof, no warnings/qualityGate. |
| `agent-artifact-job` | LEGACY | POST | Bearer AGENT_RUN_TOKEN | required | — | `inline (validateArtifactJobRequest + createArtifactJob + triggerWorker)` | LEGACY job creation. Bypasses model routing, cost receipts and the generation-budget charge that the shared path enforces (see KNOWN_ISSUES). |
| `agent-artifact-worker-background` | INTERNAL | POST, GET ?health=1 | Bearer AGENT_RUN_TOKEN | required (forwarded in the trigger body) | — | `runWorker` | Background worker: runs one artifact job (image generate/edit, PDF render/edit) and saves the artifact. |
| `capture-worker-background` | INTERNAL | POST | Bearer AGENT_RUN_TOKEN | optional and IGNORED (capture writes pdf-tool's own store) | — | `runCaptureCrawl` | Background worker: runs/resumes one site-capture crawl; chain re-triggers itself near the 15-minute budget. |
| `create-agent-artifact-job` | CURRENT | POST | Bearer AGENT_RUN_TOKEN | required | — | `createAgentArtifactJob` | HTTP mirror of `create_agent_artifact_job`. |
| `create-image-import-job` | CURRENT | POST | Bearer AGENT_RUN_TOKEN | required | — | `createImageImportJob` | HTTP mirror of `import_images_from_url` (note the different name). |
| `create-image-search-job` | CURRENT | POST | Bearer AGENT_RUN_TOKEN | required | — | `createImageSearchJob` | HTTP mirror of `search_images`. |
| `create-pdf-template` | CURRENT | POST | Bearer AGENT_RUN_TOKEN | required | — | `createPdfTemplate` | HTTP mirror of `create_pdf_template`. |
| `delete-pdf-template` | CURRENT | POST | Bearer AGENT_RUN_TOKEN | required | — | `archivePdfTemplateRecord` | HTTP mirror of `delete_pdf_template` (soft archive). |
| `get-agent-artifact-by-filename` | CURRENT | GET, POST | Bearer AGENT_RUN_TOKEN | required | — | `getAgentArtifactByFilename` | HTTP mirror of `get_agent_artifact_by_filename`. |
| `get-agent-artifact-by-slot` | CURRENT | GET, POST | Bearer AGENT_RUN_TOKEN | required | — | `getAgentArtifactBySlot` | HTTP mirror of `get_agent_artifact_by_slot`. |
| `get-agent-artifact-job-status` | CURRENT | GET, POST | Bearer AGENT_RUN_TOKEN | required | — | `getAgentArtifactJobStatus` | HTTP mirror of `get_agent_artifact_job_status`. |
| `get-image-search-job-status` | CURRENT | GET, POST | Bearer AGENT_RUN_TOKEN | required | — | `getImageSearchJobStatus` | HTTP mirror of `get_image_search_job_status`. |
| `get-pdf-template` | CURRENT | GET, POST | Bearer AGENT_RUN_TOKEN | required | — | `getPdfTemplateRecord` | HTTP mirror of `get_pdf_template`. |
| `health` | CURRENT | GET, POST | Bearer AGENT_RUN_TOKEN | none (probes pdf-tool's own store) | `/health` | `probePdfToolOwnStorage` | Blob-store health probe (`/health`). Unlike the MCP `health` tool it does NOT return the capability manifest. |
| `image-search-policy` | CURRENT | GET, POST | Bearer AGENT_RUN_TOKEN | required | — | `getImageSearchPolicy / setImageSearchPolicy` | GET = `get_image_search_policy`, POST = `set_image_search_policy`. |
| `image-search-worker-background` | INTERNAL | POST | Bearer AGENT_RUN_TOKEN | required | — | `runImageSearch / runUrlImportBatch` | Background worker: runs one image-search or url-import batch job. |
| `import-image-from-url` | CURRENT | POST | Bearer AGENT_RUN_TOKEN | required | — | `importImageFromUrl` | HTTP mirror of `import_image_from_url`. |
| `list-pdf-templates` | CURRENT | GET, POST | Bearer AGENT_RUN_TOKEN | required | — | `listPdfTemplatesResult` | HTTP mirror of `list_pdf_templates`. |
| `mcp` | CURRENT | POST, DELETE, OPTIONS, GET ?health=1 | Bearer AGENT_RUN_TOKEN, or OAuth access token, or URL connector key | per tool (see MCP_REFERENCE) | `/mcp/*`, `/mcp` | `callToolInner switch` | The MCP Streamable-HTTP JSON-RPC endpoint (`/mcp`). |
| `oauth-authorization-server` | CURRENT | GET, OPTIONS | none (public metadata) | none | `/.well-known/oauth-authorization-server*` | `authorizationServerMetadata` | RFC 8414 metadata (`/.well-known/oauth-authorization-server*`). |
| `oauth-authorize` | CURRENT | GET, POST | owner secret typed into the consent form (MCP_OAUTH_PASSWORD → MCP_CONNECTOR_KEY) | none | `/authorize` | `issueAuthorizationCode` | Consent screen + authorization-code issuance (`/authorize`), PKCE S256 mandatory. |
| `oauth-protected-resource` | CURRENT | GET, OPTIONS | none (public metadata) | none | `/.well-known/oauth-protected-resource*` | `protectedResourceMetadata` | RFC 9728 metadata (`/.well-known/oauth-protected-resource*`). |
| `oauth-register` | CURRENT | POST, OPTIONS | none (RFC 7591 dynamic registration) | none | `/register` | `registerClient` | Registers an OAuth client (`/register`); the record is stored but never consulted again. |
| `oauth-token` | CURRENT | POST, OPTIONS | authorization code + PKCE verifier, or refresh token | none | `/token` | `issueTokenPair` | Token endpoint (`/token`): 1 h access tokens, 90 d refresh tokens, HMAC-signed, no revocation list. |
| `pdf-template-preview-worker-background` | INTERNAL | POST | Bearer AGENT_RUN_TOKEN | required | — | `runPdfTemplatePreview` | Background worker: preview render + rasterize of one template version. |
| `pdf-template-thumbnail-worker-background` | INTERNAL | POST | Bearer AGENT_RUN_TOKEN | required | — | `runPdfTemplateThumbnail` | Background worker: thumbnail PNG of one published template version. |
| `pdf-template-validation-worker-background` | INTERNAL | POST | Bearer AGENT_RUN_TOKEN | required | — | `runPdfTemplateValidation` | Background worker: validation render of one template version. |
| `publish-pdf-template` | CURRENT | POST | Bearer AGENT_RUN_TOKEN | required | — | `publishPdfTemplateRecord` | HTTP mirror of `publish_pdf_template`. |
| `resume-agent-artifact-job` | CURRENT | POST | Bearer AGENT_RUN_TOKEN + operator approvalToken | required | — | `resumeAgentArtifactJob` | HTTP mirror of `resume_agent_artifact_job`. |
| `verify-agent-artifact` | CURRENT | POST | Bearer AGENT_RUN_TOKEN | optional (the only grant-optional HTTP endpoint) | — | `verifyArtifactMaterialization` | HTTP mirror of `verify_agent_artifact`. |
| `warm-ping-scheduled` | SCHEDULED | scheduled */5 * * * * | n/a (outbound only) | none | schedule `*/5 * * * *` | `fetch ×2` | Keeps `mcp` and `agent-artifact-worker-background` warm by GETting their `?health=1` routes. Other background functions are NOT warmed. |

## Worker triggers (server-to-self)

| Caller | Target background function | Trigger implementation |
|---|---|---|
| `create_agent_artifact_job`, `resume_agent_artifact_job`, legacy `agent-artifact-job` | `agent-artifact-worker-background` | `triggerWorker()` in `netlify/lib/agent-artifact-worker-trigger.ts` |
| `search_images`, `import_images_from_url` | `image-search-worker-background` | `triggerWorker(..., IMAGE_SEARCH_WORKER_FUNCTION)` |
| `create_capture_job`, `capture-worker-background` (chain resume) | `capture-worker-background` | `triggerWorker(..., CAPTURE_WORKER_FUNCTION)` |
| `validate_pdf_template`, `publish_pdf_template` (auto-validation) | `pdf-template-validation-worker-background` | `triggerValidationWorker()` in `netlify/lib/pdf-template-validation.ts` |
| `publish_pdf_template` | `pdf-template-thumbnail-worker-background` | `triggerThumbnailWorker()` in `netlify/lib/pdf-template-thumbnail.ts` |
| `preview_pdf_template` | `pdf-template-preview-worker-background` | `triggerPreviewWorker()` in `netlify/lib/pdf-template-preview.ts` |

The trigger base URL is `DEPLOY_PRIME_URL`, then `URL`, then an `Origin`/`Host` header whose hostname is in `WORKER_ORIGIN_ALLOWLIST` (`artifactWorkerBaseUrl`). Without any of these the job is created and immediately marked `failed` (`Unable to determine worker base URL`).

## render-service routes (Cloud Run)

| Route | Notes |
|---|---|
| `GET /health` | Unauthenticated liveness + engine probe; reports `build.gitSha` (`SERVICE_GIT_SHA`) which the deploy script asserts. |
| `GET /healthz` | Alias of `/health` (Cloud Run's front end intercepts the literal `/healthz` path on `*.run.app`). |
| `POST /render/typst` | Compile a typst template with `--input data=<json>`; sandboxed (`--root`, scrubbed env, vendored packages only). Auth `x-render-secret`. |
| `POST /render/chromium` | Liquid → HTML → Playwright print (JS disabled, network closed except `render.assets.invalid` virtual host and `RENDER_CHROMIUM_ALLOWED_HOSTS`). Auth `x-render-secret`. |
| `POST /rasterize/pdf` | poppler `pdftoppm` page rasterization, dpi 72–150, ≤ 40 pages, ≤ 80 Mpx/page. Auth `x-render-secret`. |
| `POST /capture/page` | Site-capture of ONE page in a JS-enabled context routed to a caller-supplied https origin allowlist; returns snapshot.v1 page + screenshots. Auth `x-render-secret`. |

Request/response contracts for these routes are validated by `render-service/src/contract.ts` (render), `rasterize.ts` (rasterize) and `capture.ts` (capture); the Netlify-side clients are `netlify/lib/pdf-render/render-service-client.ts`, `rasterize-client.ts` and `netlify/lib/capture/service-client.ts`.

