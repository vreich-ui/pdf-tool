{
  "name": "pdf-tool",
  "version": "0.1.0",
  "private": true,
  "type": "module",
  "scripts": {
    "check:eslint": "tsc --noEmit",
    "test": "npm run test:netlify",
    "test:netlify": "rm -rf .tmp-tests && tsc -p tsconfig.test.json && node --test .tmp-tests/tests/agent-artifact*.test.js"
  },
  "dependencies": {
    "@netlify/blobs": "^9.1.2",
    "@openai/agents": "^0.1.0",
    "openai": "^6.10.0",
    "zod": "^3.25.40"
  },
  "devDependencies": {
    "tsx": "^4.20.6",
    "typescript": "^5.9.3"
  }
}
